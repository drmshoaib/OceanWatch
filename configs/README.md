# Configs

Configuration files for the analysis engine live here.

`default_config.json` records the current hardcoded defaults for feature extraction, risk scoring, risk bands, and protected-area proximity. It is not wired into the command-line workflow yet; that integration is planned for the configuration-file support phase.
